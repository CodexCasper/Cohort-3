1. What are databases?
  - Databases are organized collections of data that are structured to enable efficient storage , retrieval , and management of information.
  - whenever we deply our full stack application , we use databses to store our actual data which resides in multiple stack if one fails another works
  - i.e. :
   1.TODOS of your todo app
   2.user information

2. Types of databases
- Relational Databases: Use tables to store data and relationships between data (e.g., MySQL, PostgreSQL).
- NoSQL Databases: Designed for more flexible data models and often used for big data or real-time web applications (e.g., MongoDB).
- Graph Databases: Store data in nodes and edges to represent relationships (e.g., Neo4j).
- Vector Databases: Used in ML to store data in the form of embeddings

3.MONGODB and NOSQL databases
- NoSQL databases are a broad category of database systems that diverge from the traditional relational model used in SQL databases. 
- They are designed to handle a variety of data models and workloads that may not fit neatly into the tabular schema of relational databases, means lets say if there is  nested loop in the website then this will not work
  but in some cases we can say they are better 
 

 -properties: ( schema => means how does our databses look like ) let'say ki ek todo application hai or users ka data me (email,username) will be string so this a kind of data call schema
              and we know that MONDODB is schemaless means we do not need to define what kind of info. infact it does have one 
  - schema(structure) flexibility => NOSQL databases often allow a flexible schema , which means we can store data in formats that do not require a fixed structure
  - Scalability => Many NOSQL databses are designed to scale out horizontally , making it easier to distribute data across multiple srvers and handle large volume of traffic 

 - MongoDB :
  - is a NOSQL databse that uses a document oriented approach. data is stored in flexible,JSON-like documents , which can have nested structures and vaired fields

4. Creating a free MongoDB Server
    1. Signup on https://cloud.mongodb.com/
    2. Create a `free M0` cluster
    3. Create a `User`
    4. Install MongoDB compass
    5. Put the connection URL to connect to the database


5. Seeding data in the DB - 
    - Lets put some data in the `Cluster` manually :
        1. Create a new `Database` called `todo-app`
        2. Create two collections inside it
            i. `users`
            ii. `todos`
        3. Seed some data inside the collections,'make sure the relation occurs when the objectid of one(here users) exists in other(here todos)'

6. CRUD operations
    - CRUD operations in MongoDB refer to the basic operations you can perform on documents within a MongoDB database. CRUD stands for:
        1. Create: Adding new documents to a collection.
        2. Read: Retrieving documents from a collection.
        3. Update: Modifying existing documents in a collection.
        4. Delete: Removing documents from a collection.        
